{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Backend only — no frontend build'"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
