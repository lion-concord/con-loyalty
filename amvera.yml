meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  image: node:20-alpine
  script:
    - npm ci --omit=dev || npm install --production
run:
  image: node:20-alpine
  script:
    - node bot.cjs
  persistenceMount: /data
