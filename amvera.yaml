meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20
build:
  skip: false
  commands:
    - npm install
    - npm run build:server
run:
  persistenceMount: /data
  containerPort: 80
  scriptName: start
